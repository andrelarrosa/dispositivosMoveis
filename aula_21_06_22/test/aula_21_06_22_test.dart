import '../aula/Bola.dart' as bola;
import '../aula/Camiseta.dart' as camiseta;
import '../aula/Luva.dart' as luva;
import '../aula/Aluno.dart' as aluno;

void main() {
  // var b = new bola.Bola();
  // var lv = new luva.Luva();
  // var c = new camiseta.Camiseta(tamanho: 7.0);

  // print(b.cor);
  // print(b.tamanho);

  // print('====================');

  // c.cor = 'preto';
  // c.tamanho = 12;

  // print(c.cor);
  // print(c.tamanho);

  // print('====================');

  // lv.cor = 'preto';
  // lv.tipoTecido = "sei la";

  // print(lv.cor);
  // print(lv.tipoTecido);
}
// → qual a diferença em fazer a inicialização no atributo e no construtor? 
// No atributo você está definindo um valor padrão ao atributo, já no construtor não é definido um valor padrão, em todo lugar no sistema vc pode definir outro somente digitando no construtor
// → o que é e para que serve o construtor? qual código pode-se definir nele?
// É o que utilizamos para instanciar um objeto. Podemos definir atributos nele.
