import 'package:flutter/material.dart';

class TarefaForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("Cadastro de Tarefas")));
  }
}
